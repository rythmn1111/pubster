# Auto-install peer dependencies (pnpm default, pinned here for clarity).
auto-install-peers=true
# Don't hard-fail installs on peer-dependency mismatches; we vet versions manually.
strict-peer-dependencies=false
