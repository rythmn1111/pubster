import type { FastifyPluginAsync } from 'fastify';

/**
 * auth routes — Phase 1.
 *
 * SKELETON: registers no routes yet. Endpoints, zod schemas (schema.ts) and
 * business logic (service.ts) land in a follow-up task
 * (see docs/BACKEND.md §API surface).
 */
const authRoutes: FastifyPluginAsync = async (_app) => {
  // no routes registered yet
};

export default authRoutes;
