# Base URL of the Pubster backend API (see docs/BACKEND.md — REST under /api/v1).
# The dashboard reads this at build/runtime; it must be a full origin + prefix.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
